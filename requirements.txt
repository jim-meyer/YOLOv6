# pip install -r requirements.txt
# python3.8 environment

torch>=1.8.0
torchvision>=0.9.0
numpy==1.24.4
# numpy>=1.20 and <=1.24.0 causes this:
#   `np.float` was a deprecated alias for the builtin `float`. To avoid this error in existing code, use `float` by itself. Doing this will not modify any behavior and is safe. If you specifically wanted the numpy scalar type, use `np.float64` here.
#   The aliases was originally deprecated in NumPy 1.20; for more details and guidance see the original release note at:
#       https://numpy.org/devdocs/release/1.20.0-notes.html#deprecations
# Earlier versions of numpy result in:
#   ValueError: numpy.ndarray size changed, may indicate binary incompatibility. Expected 88 from C header, got 80 from PyObject
# numpy<1.20
# Per https://stackoverflow.com/questions/64634520/attributeerror-partially-initialized-module-cv2-has-no-attribute-cascadeclas, to avoid:
#  AttributeError: partially initialized module 'cv2' has no attribute 'gapi_wip_gst_GStreamerPipeline' (most likely due to a circular import)
opencv-python==4.5.5.64
# opencv-python>=4.1.2
PyYAML>=5.3.1
scipy>=1.4.1
tqdm>=4.41.0
addict>=2.4.0
tensorboard>=2.7.0
pycocotools>=2.0
onnx>=1.10.0  # ONNX export
# JIMM BEGIN - this doesn't install on MacOS
#onnx-simplifier>=0.3.6 # ONNX simplifier
# JIMM END
thop  # FLOPs computation
# pytorch_quantization>=2.1.1

# JIMM BEGIN
# setuptools==59.6.0 does not work:
#   AttributeError: module 'distutils' has no attribute 'version'
setuptools==59.5.0

# Avoids:
#   ImportError: cannot import name 'ParamSpec' from 'typing_extensions' (/opt/conda/lib/python3.8/site-packages/typing_extensions.py)
typing_extensions==4.8.0

# matplotlib>=3.2.2
# Pillow>=7.1.2
# requests>=2.23.0
# protobuf<4.21.3
# pandas>=1.1.4
# seaborn>=0.11.0
#
# setuptools==59.5.0

# torch-ort-infer==1.12.0
# openvino==2022.1.0
# openvino-dev==2022.1.0
# openvino-telemetry==2022.1.0
# JIMM END